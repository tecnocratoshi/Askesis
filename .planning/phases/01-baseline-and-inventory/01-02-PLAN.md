---
phase: 01-baseline-and-inventory
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md
  - .planning/phases/01-baseline-and-inventory/01-BASELINE-CMDLOG.txt
autonomous: true
requirements:
  - BASE-02
  - BASE-03
must_haves:
  truths:
    - "O time consegue repetir a baseline minima com comandos explicitos e comparar resultados."
    - "Cada evidencia no relatorio aponta para arquivo, comando e artefato verificavel."
    - "O indice consolidado de evidencia fica organizado por nivel de risco sem fragmentacao de arquivos canonicos."
  artifacts:
    - path: ".planning/phases/01-baseline-and-inventory/01-BASELINE-CMDLOG.txt"
      provides: "Log bruto reproduzivel de execucao baseline"
      contains: "npm ci / npm run typecheck / npm test"
    - path: ".planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md"
      provides: "Baseline Execution preenchido + Evidence Index por risco"
      contains: "Evidence Index"
  key_links:
    - from: ".planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md"
      to: ".planning/phases/01-baseline-and-inventory/01-BASELINE-CMDLOG.txt"
      via: "campo artifact em cada evidencia"
      pattern: "artifact"
    - from: ".planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md"
      to: "BASE-02"
      via: "secao Baseline Execution com comandos literais e status"
      pattern: "npm run typecheck|npm test"
---

<objective>
Executar a baseline minima reproduzivel da fase e consolidar evidencias rastreaveis por risco.

Purpose: Transformar a estrutura definida no plano 01-01 em evidencias verificaveis, prontas para alimentar classificacao de achados na Fase 2.
Output: `01-BASELINE-CMDLOG.txt` com execucao baseline e `01-BASELINE-INVENTORY-EVIDENCE.md` atualizado com Evidence Index completo.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-baseline-and-inventory/01-CONTEXT.md
@.planning/phases/01-baseline-and-inventory/01-RESEARCH.md
@.planning/phases/01-baseline-and-inventory/01-01-PLAN.md
@package.json
@tests/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Executar baseline minima e registrar log bruto reproduzivel</name>
  <files>.planning/phases/01-baseline-and-inventory/01-BASELINE-CMDLOG.txt, .planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md</files>
  <action>Executar baseline minima conforme D-04 e D-05 usando `npm ci`, `npm run typecheck` e `npm test`. Registrar no arquivo de log bruto os comandos, timestamp, exit code e blocos de saida relevantes. Atualizar a secao `Baseline Execution` no relatorio consolidado com status por comando e referencia para trechos do cmdlog.</action>
  <verify>
    <automated>npm run typecheck && npm test && rg -n "^timestamp:|^exit_code:|npm ci|npm run typecheck|npm test" .planning/phases/01-baseline-and-inventory/01-BASELINE-CMDLOG.txt && rg -n "^## Baseline Execution|cmdlog|timestamp|exit code|npm run typecheck|npm test" .planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md</automated>
  </verify>
  <done>Baseline minima executa sem ambiguidade e o relatorio consolidado referencia onde cada resultado pode ser revalidado no cmdlog.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar regra de deferimento para build/checagens extras com decisao registrada</name>
  <files>.planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md</files>
  <action>Aplicar regra objetiva para D-06: `build_required=true` quando houver mudanca em qualquer caminho de runtime/build surface (`index.tsx`, `render/**`, `listeners/**`, `services/**`, `api/**`, `sw.js`, `build.js`, `package.json`, `tsconfig*.json`); caso contrario `build_required=false`. Registrar explicitamente na secao `Open Gaps and Assumptions` os campos `build_required`, `trigger_matched`, `decision_rationale` e `promote_rule`. Quando `build_required=true`, executar `npm run build` e registrar resultado no cmdlog + Baseline Execution. Quando `false`, registrar deferimento com criterio de promocao para fases seguintes.</action>
  <verify>
    <automated>rg -n "D-06|build_required|trigger_matched|decision_rationale|promote_rule|Open Gaps and Assumptions" .planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md</automated>
  </verify>
  <done>Decisao de build esta registrada com gatilho objetivo e campos verificaveis; quando exigido, resultado de build tambem fica registrado no baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Preencher Evidence Index por risco com ligacoes concretas para arquivo e artefato</name>
  <files>.planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md</files>
  <action>Preencher o `Evidence Index` por nivel de risco (D-08) com entradas estaveis `E-01-*`, cobrindo no minimo BASE-02 e BASE-03. Cada entrada deve conter `requirement_ref`, `target` (arquivo concreto), `command`, `artifact` (secao/linha do cmdlog ou resultado do comando), `reproducibility` e `status`, mantendo o relatorio consolidado como fonte canonica (D-07, D-09).</action>
  <verify>
    <automated>rg -n "E-01-|BASE-02|BASE-03|requirement_ref|target|command|artifact|reproducibility|status" .planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md</automated>
  </verify>
  <done>Indice de evidencia esta completo, organizado por risco e rastreavel para arquivos e artefatos verificaveis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command-runner -> baseline-report | Saidas de comandos entram no relatorio e podem perder fidelidade se nao houver ponteiro para artefato bruto. |
| evidence-entry -> requirement-traceability | Entradas de evidencia precisam manter vinculo estrito com BASE-02/BASE-03 e arquivos reais. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | resultados baseline | mitigate | Registrar comandos literais e preservar `01-BASELINE-CMDLOG.txt` como artefato bruto referenciado no relatorio. |
| T-01-05 | Repudiation | evidence index | mitigate | Exigir `evidence_id` estavel e `requirement_ref` por entrada para permitir auditoria cruzada com REQUIREMENTS.md. |
| T-01-06 | Denial of Service | execucao de baseline | accept | Falhas de ambiente podem impedir execucao; risco aceito na fase de planejamento com necessidade de retentativa no executor. |
</threat_model>

<verification>
Validacao automatizada de fechamento do plano:
- `npm run typecheck && npm test`
- `rg -n "Evidence Index|E-01-|BASE-02|BASE-03" .planning/phases/01-baseline-and-inventory/01-BASELINE-INVENTORY-EVIDENCE.md`
- `rg -n "npm ci|npm run typecheck|npm test" .planning/phases/01-baseline-and-inventory/01-BASELINE-CMDLOG.txt`
</verification>

<success_criteria>
1. Baseline minima (typecheck + test) foi executada e registrada com comandos reproduziveis.
2. Decisao de build/checagens extras esta documentada de forma consistente com D-06.
3. Evidence Index consolidado liga cada observacao a requisitos, arquivos e artefatos verificaveis.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-inventory/01-02-SUMMARY.md`
</output>
